import React from 'react';
import { DataGrid } from '@mui/x-data-grid';
import FileButton from './FileButton';

const rows = [
  { id: 1, vendor: 'Vendor 1', server: 'Server 1', file: 'File 1' },
  { id: 2, vendor: 'Vendor 2', server: 'Server 2', file: 'File 2' },
  { id: 3, vendor: 'Vendor 3', server: 'Server 3', file: 'File 3' },
  // Add more rows as needed
];

const columns = [
  { field: 'id', headerName: 'ID', width: 70 },
  { field: 'vendor', headerName: 'Vendor', width: 130 },
  { field: 'server', headerName: 'Server', width: 130 },
  {
    field: 'file',
    headerName: 'File',
    width: 130,
    renderCell: (params) => (
      <FileButton rowId={params.row.id} onClick={(rowId) => handleFileButtonClick(rowId)} />
    ),
  },
  // Add more columns as needed
];

const handleFileButtonClick = (id) => {
  console.log('Row ID:', id);
  // You can now use the ID as needed
};

const MyDataGrid = () => {
  return (
    <div style={{ height: 400, width: '100%' }}>
      <DataGrid
        rows={rows}
        columns={columns}
        pageSize={5}
      />
    </div>
  );
};

export default MyDataGrid;
