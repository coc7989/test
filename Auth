import React, { useState } from 'react';
import Switch from '@mui/material/Switch';
import { DataGrid } from '@mui/x-data-grid';

const YourComponent = ({ compareData }) => {
  const [showAllData, setShowAllData] = useState(false);
  
  const filteredData = compareData.filter(row => showAllData || row.difference !== 0);
  
  const columns = [
    { field: 'file1', headerName: 'File 1', width: 150 },
    { field: 'file2', headerName: 'File 2', width: 150 },
    { field: 'difference', headerName: 'Difference', width: 150 },
  ];

  return (
    <div>
      <Switch
        checked={showAllData}
        onChange={() => setShowAllData(!showAllData)}
        inputProps={{ 'aria-label': 'Toggle' }}
      />
      <span>Show All Data</span>
      <div style={{ height: 400, width: '100%' }}>
        <DataGrid
          rows={filteredData}
          columns={columns}
          pageSize={5}
          rowsPerPageOptions={[5]}
          disableSelectionOnClick
        />
      </div>
    </div>
  );
};

export default YourComponent;
