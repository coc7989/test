SELECT vendor, server, expiry_date
FROM expiry_table
WHERE (vendor, expiry_date) IN (
    SELECT vendor, MAX(expiry_date) AS max_expiry_date
    FROM expiry_table
    GROUP BY vendor
)
ORDER BY expiry_date;
