import subprocess

vendor_name = "your_vendor_name"
feature_name = "your_feature_name"

command = ["/home/data", "-c", vendor_name, feature_name]
result = subprocess.check_output(command).decode("utf-8")

print(result)
